name: 12 - Custom composite action
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  composite:
    runs-on: ubuntu-latest
    name: Npm build test 2
    steps:
      - uses: actions/checkout@v4
      - id: compuesta
        uses: juan-sanchez-ait/gha-npm-composite-action@v2
        with:
          node-version: 20
